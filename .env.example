# The base URL of the Kilo Remote server.
# Copy this file to .env and replace the value with your server's URL.
# The `EXPO_PUBLIC_` prefix is required by Expo to expose the variable to the client-side app.
EXPO_PUBLIC_DEFAULT_WORKSPACE_URL=http://localhost:3000